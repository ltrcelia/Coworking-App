import {MemberModel} from "../model/member-model.ts";

describe('Create Member Action', () => {
    it('should create a new member', () => {
        const store = createTestStore(); /* TODO create store*/

        const form: MemberModel.MemberForm = {
            /* TODO create store*/
        }
    })
})
